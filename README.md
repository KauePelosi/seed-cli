# Seed

The idea is build a program who create automatically your project structure per language, so the user call the executable and tell the language and project name.

**Example:** seed cpp my_project

And the program will create **all your files** and initialize a git repository.

# How Works

The program will use templates to define what kind of structure create and the user can to manage your own templates so the project will be flexible and a lot of useful.
